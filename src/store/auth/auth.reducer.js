import * as actions from './auth.actions';

const initState = {

};

export default (state = initState, action) => {

    switch(action.type) {
        // case (actions.)
        default: return state;
    }

};